import { ScreenPlaceholder } from '@/components/common/ScreenPlaceholder';

export default function OnboardingScreen() {
  return (
    <ScreenPlaceholder
      description="Account setup and personal information will be added in a later implementation prompt."
      eyebrow="Getting started"
      title="Onboarding"
    />
  );
}
